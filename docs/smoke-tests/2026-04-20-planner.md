# 2026-04-20 — Planner smoke test (Tasks 5–8 end-to-end)

Second smoke test after the baseline. Validates the **full** pipeline:
issue labeled `plan` → Planner → change-set comment → Implementer
auto-dispatch → PR → board transitions → merge → Done.

## Environment

- **Repo / org:** `herbae-org/foreman` / `herbae-org`
- **Board:** `herbae-org/projects/2` "AI Factory"
- **HEAD at test:** `8d6771c`
- **Secrets:** `AGENT_PAT`, `ANTHROPIC_API_KEY`. `notifications.provider: none`.
- **Planner model:** `claude-sonnet-4-6` / **Implementer model:** `claude-opus-4-6`

## Seed issue

[#4 docs(architecture): document the Planner agent (workflow + prompt)](https://github.com/herbae-org/foreman/issues/4) — added to the board as `Todo`, labeled `p2`, then labeled `plan` to trigger the Planner.

## Runs

| # | Workflow | Run | Outcome |
|---|---|---|---|
| 1 | Planner | [`24680581924`](https://github.com/herbae-org/foreman/actions/runs/24680581924) | ✅ Classified `low`, posted change-set, applied `risk/low`, removed `plan`, dispatched Implementer |
| 2 | Implementer | [`24680644485`](https://github.com/herbae-org/foreman/actions/runs/24680644485) | ✅ Fetched change-set from issue comment, added §3.0 + §4.0 to ARCHITECTURE.md, opened PR |
| 3 | Board-Sync (PR opened) | fired on `pull_request.opened` | ✅ `Todo → Ready For QA` |
| 4 | Board-Sync (PR merged) | fired on `pull_request.closed` | ✅ `Ready For QA → Done` |

PR #5: <https://github.com/herbae-org/foreman/pull/5> (merged 17:xx UTC).

## Quality observations

- **Planner change-set** was unusually well-written — included an "Implementation notes for the Implementer" appendix with the literal text blocks to insert. The Implementer clearly used them: the final diff matches the intended section headings and prose style of the existing `### 3.x` / `### 4.x` sections.
- **Risk classification** was `low`, matching the rubric's docs-only default. No edge cases triggered.
- **`<!-- AUTOAGENT_CHANGE_SET -->` marker** worked as designed — the Implementer's `tail -n +2` yielded a clean change-set body identical to what the Planner wrote to `/tmp/change-set.md`.

## Bugs surfaced

1. **Implementer `Comment on success` race condition** — the step's `gh pr list --search` call ran ~6s after `gh pr create` and lost the race against GitHub's search index. Result: step exits 0 but no "Implementation complete. PR: ..." comment posted on the issue. Parked in `docs/parking-lot.md`. Not blocking — PR opens and board transitions regardless.

## Conclusion

The four-agent pipeline (Planner, Implementer, Fixer, Merger) — or at least the Planner → Implementer → Board-Sync arc of it — works end-to-end against the real repo with zero manual intervention between `plan` label add and `Done` column. Fixer and Merger still need a bad PR / failing CI to exercise; those are separate test scenarios.
